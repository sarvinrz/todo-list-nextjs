import React from 'react';
import styles from '@/styles/Home.module.css';

const FilterButtons = function () {
  return (
    <main className={`${styles.main}`}>
      <div className='filter-buttuns'>hello</div>
    </main>
  );
};

export default React.memo(FilterButtons);
